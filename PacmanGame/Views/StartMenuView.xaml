<UserControl x:Class="PacmanGame.Views.StartMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PacmanGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Pacman 2000" Grid.Row="0"/>
        <Button Content="Nowa gra" Command="{Binding NewGameCommand}" Grid.Row="1"/>
        <Button Content="Wczytaj grę" Command="{Binding LoadGameCommand}" Grid.Row="2"/>
        <Button Content="Najlepsze wyniki" Command="{Binding ShowHighscoresCommand}" Grid.Row="3"/>
        <Button Content="Ustawienia" Command="{Binding ShowOptionsCommand}" Grid.Row="4"/>
        <Button Content="Pomoc" Command="{Binding ShowHelpCommand}" Grid.Row="5"/>
        <Button Content="Wyjście" Command="{Binding ExitCommand}" Grid.Row="6"/>
    </Grid>
</UserControl>
