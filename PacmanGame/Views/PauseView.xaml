<UserControl x:Class="PacmanGame.Views.PauseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PacmanGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Text="Pauza"/>
        <Button Content="Kontynuuj grę" Command="{Binding ReturnCommand}"/>
        <Button Content="Zapisz grę" Command="{Binding SaveGameCommand}"/>
        <Button Content="Powrót do menu" Command="{Binding BackToMenuCommand}"/>
    </StackPanel>
</UserControl>
